# AI 利用記録 (ch02/ex05: セミコロンの省略)

- 状況: `index.test.js` が何を確認するテストか分からなかった。`index.js` を壊しても落ちない。
- 聞いたこと: このテストは何をテストしているのか。
- 結果: セミコロンを削っても挙動が変わらないことの確認だと整理できた。
  `promisify(exec)` の戻り値がオブジェクトのため両辺が `"[object Object]"` になり
  常にパスしていたので、分割代入で直す。
