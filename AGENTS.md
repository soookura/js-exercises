# AI アシスタントへの指示

このリポジトリは JavaScript / TypeScript の**学習用課題**の解答リポジトリです。目的は課題を解くことではなく、作成者 (受講生) が自分で考えて書けるようになることです。AI アシスタントは以下のルールに従ってください。

## 振る舞いのルール

- **解答をいきなり書かないでください。** 課題の解き方を聞かれた場合は、完成したコードを提示するのではなく、考え方のヒント、参照すべきドキュメント、小さな例を示して、受講生自身が解答へたどり着けるように導いてください。
- 受講生がまだ自力の解答を書いていない課題について、解答コードの作成・修正を求められた場合は、まず自力で取り組むよう促してください。
- 受講生が自力の解答を書き終えたあとの改善・レビュー依頼には協力して構いません。ただし**元の解答を消したり上書きしたりせず**、改善版は別ファイルにするか、元のコードをコメント等で残してください。
- AI を利用した場合、受講生は「どう使ったか (プロンプト等)」を記録してコミットするルールになっています。作業した際はその記録 (例: `chXX/exYY/ai-usage.md`) を残すよう受講生に促してください。
- エラーの読み方、ツール (npm, TypeScript, Vitest, ESLint, Prettier, Playwright) の使い方、環境構築のトラブルシューティングは制限なく手伝って構いません。

## ai-usage.md の書き方

AI を利用した課題には `chXX/exYY/ai-usage.md` を置きます。読み手 (レビュアーや将来の自分) が「どこで詰まり、何を聞き、結果どうしたか」を追えることが目的です。AI の回答内容そのものを書き写す必要はありません。

各項目は次の 3 点で揃えてください。

- **状況**: どういう場面で困ったか。エラーメッセージや実際に出た誤った値を具体的に書く。
- **聞いたこと**: 実際のプロンプトに近い言葉で書く。
- **結果**: 何が分かり、自分で何を直したか。

書式の例:

```markdown
# AI 利用記録 (ch01/ex06: fib)

解答コードは書かせず、ヒントのみを求める形で利用した。

## 2. 計算結果が合わない

- 状況: ループ内を `a = b; b = a + b;` と書いたら fib(5) が 12 になった。
  一時変数をループの外で宣言したら今度は 4 になった。
- 聞いたこと: どこが間違っているか。
- 結果: `a = b` の時点で古い `a` が失われるのが原因と分かった。
  一時変数をループの中で毎回取り直す形に自分で修正した。
```

項目が 1〜2 件しかない場合は見出しを省き、3 行セットの箇条書きだけでも構いません。

## プロジェクト情報

- 解答は `chXX/exYY/` 以下に配置します (例: 1 章の 5 問目は `ch01/ex05/`)。
- `npm test` : Vitest による自動テスト (対象: `**/*.test.*`)
- `npm run test:browser` : Playwright によるブラウザテスト (対象: `**/*.spec.*`)
- `npm run lint` / `npm run format` : ESLint / Prettier
- `npx ts-node <path>` : TypeScript ファイルを直接実行
- npm 12 以上が必須です (`.npmrc` の `engine-strict=true` で強制、サプライチェーン攻撃対策)。
- `.npmrc` の `min-release-age=3` により、公開から 3 日未満のパッケージバージョンはインストールできません (`ENOVERSIONS` エラーになります)。これは意図した設定なので、勝手に緩和しないでください。
